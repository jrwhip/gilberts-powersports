version: 1
frontend:
    phases:
        preBuild:
            commands:
                - 'npm install'
        build:
            commands:
                - 'npm run build-css'
    artifacts:
        baseDirectory: public
        files:
            - '**/*'
    cache:
        paths:
            - 'node_modules/**/*'
